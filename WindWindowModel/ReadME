# Wind Window Model - Works only on MacOS

#Program for visualisation of kite wind window with colors corresponding to the power generated by kite movement
#Program contains simple particles system connected with springs. Modelled rope imitates tether connecting kite with pilot

#For succesful run we need to import libraries: ToxicLibs and PeasyCam only

#We can import libs using ProcessingIDE Sketch > Import libraries > "find two mentioned"
